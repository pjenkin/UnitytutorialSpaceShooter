using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // 6-43 move enemy object downwards at 4 units/metres per second
        // when off the bottom of the screen, respawn object at top at a random x position
    }
}
